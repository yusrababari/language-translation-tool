Language Translation Tool
-Includes a user interface which allows the user to enter text and translate it into chosen language
-Uses an API to process the input
-Sends the text tot he API and gets the translated respinse
-Displays the translated text clearly on the screen